.row
  .columns
    h2 = title("Sign up")

.row
  .medium-5.columns
    = simple_form_for resource,
        as: resource_name,
        url: registration_path(resource_name) do |f|

      legend
        | Create your
        '
        strong Rails Base example site
        '
        | account

      .form-inputs
        = f.input :full_name, input_html: {value: "John Smith"} , required: true, autofocus: true
        = f.input :email, input_html: {value: "john@example.com"}, required: true
        = f.input :password, required: true
        = f.input :password_confirmation, required: true

      .form-actions
        = f.button :submit, "Sign up"

  .medium-5.medium-offset-1.columns.end
    = render "users/shared/links"
